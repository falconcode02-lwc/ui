export enum Visibility {
    none,
    dbview,
    codeeditor,
    consteditor,
    schedulereditor,
    apieditor
}

export enum NodeType {
    none,
    object,
    function,
    model,
    condition,
    switch,
    constants,
    api,
    scheduler,
    controller,
    plugin
}